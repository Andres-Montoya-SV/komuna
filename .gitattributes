# Normalize text files to LF in the repo and working tree (avoids single-line / CRLF issues on GitHub).
* text=auto

*.yaml text eol=lf
*.yml text eol=lf
*.sh text eol=lf

backend/requirements.txt text eol=lf
backend/requirements-dev.txt text eol=lf
